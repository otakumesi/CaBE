task: predict
  
ex:
  file_name: 'reverb45k_test'
  lm_name: BERT

model:
  num_layer: 
  threshold: .01
  linkage: average
  similarity: cosine

grid_search:
  num_process: 8
  min_threshold:  0.01
  max_threshold:  0.3
  threshold_step: 0.01
  min_layer: 0
  max_layer:
  similarity: cosine
  linkages:
    - average
    - single
    - complete
