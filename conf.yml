task: predict
  
ex:
  file_name: 'reverb45k_test'
  enc: BERT
  similarity: cosine

model:
  num_layer: 
  threshold: .01
  linkage: average
  cluster_size: 2

grid_search:
  num_process: 8
  min_threshold:  0.0
  max_threshold:  0.15
  threshold_step: 0.01
  min_layer: 0
  max_layer:
  linkages:
    - average
    - single
    - complete

vis:
  n_min_elems:
  n_max_elems:
